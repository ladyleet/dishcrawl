import dishesRochester from '../../../utils/dishes-rochester';
import { module, test } from 'qunit';

module('Unit | Utility | dishes rochester');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dishesRochester.length;
  assert.ok(result);
});
