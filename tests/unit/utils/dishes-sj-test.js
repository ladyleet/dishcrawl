import dishesSj from '../../../utils/dishes-sj';
import { module, test } from 'qunit';

module('Unit | Utility | dishes sj');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dishesSj();
  assert.ok(result);
});
