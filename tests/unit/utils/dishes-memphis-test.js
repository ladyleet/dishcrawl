import dishesMemphis from '../../../utils/dishes-memphis';
import { module, test } from 'qunit';

module('Unit | Utility | dishes memphis');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dishesMemphis();
  assert.ok(result);
});
