import dishesDc from '../../../utils/dishes-dc';
import { module, test } from 'qunit';

module('Unit | Utility | dishes dc');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dishesDc();
  assert.ok(result);
});
