import dishesMontreal from '../../../utils/dishes-montreal';
import { module, test } from 'qunit';

module('Unit | Utility | dishes montreal');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dishesMontreal();
  assert.ok(result);
});
