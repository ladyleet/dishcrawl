import dishesLa from '../../../utils/dishes-la';
import { module, test } from 'qunit';

module('Unit | Utility | dishes la');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dishesLa();
  assert.ok(result);
});
