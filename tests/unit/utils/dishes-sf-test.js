import dishesSf from '../../../utils/dishes-sf';
import { module, test } from 'qunit';

module('Unit | Utility | dishes sf');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dishesSf.length;
  assert.ok(result);
});
