import dishesNyc from '../../../utils/dishes-nyc';
import { module, test } from 'qunit';

module('Unit | Utility | dishes nyc');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dishesNyc.length;
  assert.ok(result);
});
